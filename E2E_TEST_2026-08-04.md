# End-to-end re-run — 2026-08-04

Everything since 3 August had been verified in isolation or through
`build_summary`. This run drives `run_pipeline` against the live instance, which
is the only path that exercises the Import Studio profile fetch, the SQLite
persistence, the SENAITE push and the review plan.

Target: worksheet **WS-0005** (288 KCP FEED analyses), method FDA_32PFAS,
matrix Animal Feed. `batch_id="WS-0005"` reused exactly — `persist_qc_results`
supersedes by batch id, and the database showed 564 prior rows under that
string. ZODB (193 MB) and the QC database snapshotted first.

## Stages reached

| Stage | Result |
|---|---|
| Import Studio profile fetch | **pass** — resolved `native:(no version)`, 1254 rows / 22 injections |
| Salt correction | **pass** — 9 rows, PFOA × 0.9636. First application in a real run |
| Matrix factor | **pass** — 306 native rows; standards and CCVs correctly untouched |
| QC engine | **pass** — 582 flags, identical to the isolated measurement |
| Unconfigured criteria | **0** — no deviation raised, correct for a fully configured profile |
| Persistence | **pass after a fix** — 1254 injection rows, 496 QC results, 34 curves |
| SENAITE push | **pass** — 224 of 224 results filed (288 before the batch link was supplied; dilutions then fold into their parents) |
| Report PDF | **pass** — `/data/qc/batches/WS-0005_report.pdf` |
| Data Review gates | traceability **pass**, qc_summary **fail** — 43 failing across IS/LFSM/LFSMD (the fixture's incoherent spikes) |
| CoA / EDD | **not reached** — correctly blocked by the failing QC gate |

## Findings

### F1 — QC persistence collided on repeated injection names (FIXED)

`qc_results` is unique on `(batch_id, analyte, qc_type, qc_level)` and the IS
rows used `qc_level = injection_name`. **This export contains three CCV
injections all named `FDA-CCV-251020`**, distinguished only by acquisition time,
so three IS rows per compound shared a key: `UNIQUE constraint failed`.

Two defects, not one:

1. The key assumed injection names are unique within a run. Real instrument data
   does not honour that. `concat_id` — documented as
   `"injection_name|yyyymmddHHMMSS (unique key)"` — already existed for exactly
   this and was not being used.
2. **The failed insert rolled back and left the PREVIOUS run's 496 rows in
   place**, reported only as one ERROR line. Data Review would have gated on
   stale QC and said nothing. The persist now clears the batch's rows on
   failure, so the gate reports "no QC record" and blocks — the honest state.

Masked until today because the relabelled fixture gave the CCVs distinct names.
A fixture that tidies the data hides the defects the data would have found.

### F2 — WITHDRAWN. My invocation error, not a product defect

Reported as "LFSM/LFSMD blocked with no resolution path". It was neither
blocked nor without a path: **I omitted `senaite_batch_id` when calling
`run_pipeline`.** The extraction pedigree is fetched only when that argument is
present, so `batch.spikes` was empty — and so was `dilution_map`, meaning the
1:10 dilution handling never ran either. The pedigree was complete all along:
100 ppt with the parent named, for both LFSM and LFSMD.

`pending_spike_levels()` returning 0 was therefore *correct* — nothing was
pending.

Re-run with `senaite_batch_id="kcp-b-001"`:

| | without the batch link | with it |
|---|---|---|
| Extraction pedigree | not fetched | 2 spikes @ 100 ppt |
| Dilutions | not fetched | 2 recorded, QC-skipped, IS still checked |
| QC flags | 582 | 545 |
| QC results | 496 (IS, MB) | 564 (IS, MB, **LFSM 34, LFSMD 34**) |
| Results pushed | 288/288 | **224/224** — matches the 3 Aug baseline |
| Gate reason | "required QC not evaluated" | "43 failing across IS, LFSM, LFSMD" |

The lesson is about the harness rather than the product: a partially-specified
invocation produced a plausible, complete-looking run — right up to a QC gate
whose message pointed at the wrong cause. Worth a defensive check that
`senaite` without `senaite_batch_id` warns rather than silently skipping the
pedigree.

**LFSM failing 34/34 is the fixture, not a defect** — this export's spike
arithmetic is incoherent, which is why the QC-coherent companion was derived
on 3 August.

### F3 — the relabelled fixture no longer matches the SENAITE samples

First run pushed only 32 of 288: the samples carry the ORIGINAL injection names
as ClientSampleID (`KCP Silage "Egg-1" Sample`) while the relabelled fixture uses
`FDA_32PFAS-FEED-0002`. Fixture/state drift, not a code defect — re-running with
the original export gave 288/288. Worth deciding which of the two fixtures is
canonical, because they now test different things.

## Not reached

CoA publication and EGAD EDD export. The QC gate fails, so publication is
blocked — the correct behaviour, and reported as blocked rather than worked
around.
